VITE_API_BASE_URL=http://localhost:5000
# Production Free build uses the exact deployed origin and same-origin managed API.
# For local cookie tests use a matching local origin with a local /api gateway.
VITE_COOKIE_PORTAL_ORIGIN=https://lively-sea-0776c9a0f.6.azurestaticapps.net
VITE_COOKIE_API_BASE_URL=
