services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: orobi
      POSTGRES_USER: orobi
      POSTGRES_PASSWORD: ${OROBI_POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - orobi-postgres:/var/lib/postgresql/data

volumes:
  orobi-postgres:
